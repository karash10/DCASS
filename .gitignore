# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# Virtual environments
.env
.venv/
venv/
ENV/
env/

# Storage directory - runtime data (keep structure, ignore contents)
storage/*
!storage/.gitkeep

# Legacy data directories (if any remain)
data/raw/
data/indices/
data/cache/
data/processed/
data/audio
data/benchmarks/

# Model files (now in storage/, download or train locally)
*.pt
*.pth
*.onnx
*.bin

# Jupyter notebooks checkpoints
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~
nul  # Windows-specific: accidental redirection file

# Logs (now in storage/logs/)
*.log

# Build artifacts
build/
dist/
*.egg-info/
*.egg

# IDE settings
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Type checking
.mypy_cache/
.pytype/

# Phase 3 output (moved to storage/outputs/)
# (keeping for backward compatibility if old files exist)
phase3_out/

# Secrets (never commit!)
*.pem
*.key
credentials.json
secrets.yaml
.env.local
.env.*.local
api_keys.json
config.local.yaml

# Claude Code private files (DO NOT COMMIT TO PUBLIC REPO)
.claude/
Claude.md
CLAUDE.md

# Docker simulation artifacts (now in storage/)
# (keeping for backward compatibility)
shared_channel/
checkpoints/

# Docker build cache and artifacts
.docker/
docker-compose.override.yml
*.tar
*.tar.gz

# Training artifacts (now in storage/data/behavioral/)
# (keeping for backward compatibility)
data/behavioral/
tensorboard_logs/
runs/

# Audio files (large)
*.wav
*.mp3
*.flac
*.ogg

# HuggingFace cache
.cache/
huggingface/

# Temporary files
*.tmp
*.temp
*.bak
*.swp
*~

# Private notes and drafts
NOTES.md
TODO_PRIVATE.md
drafts/
personal/

# OS-specific
.DS_Store
Thumbs.db
Desktop.ini
*.lnk

# Editor directories
.idea/
.vscode/
*.sublime-*
*.code-workspace
